# osu-beatmap-cleaner

A Windows utility to clean your osu! Songs folder by:

- Filtering mapsets/difficulties via BPM / AR / CS
- Removing mapsets not referenced in `collection.db`

Workflow:
1. Scan your Songs folder
2. Review everything marked for deletion
3. Confirm permanent deletion

Supports Windows standalone EXE builds via PyInstaller.
